import React from "react";



export default function Reply() {
  // Need to handle user login before doing this.
  // Had idea to have replies darken from amount of clicks.
  // dynamically change text color to maximize contrast.
  // maybe shake the color values by user tripcode to add a unique flair between users.
}